let stato = "errore";


if (stato === "errore") {
  console.error("Si è verificato un errore!");

} else if (stato === "avviso") {

  console.warn("Questo è solo un avviso!");
  
  console.log("Tutto è a posto!");
}
